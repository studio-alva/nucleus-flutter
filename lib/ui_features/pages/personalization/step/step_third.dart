import 'package:flutter/material.dart';

class StepThirdPerson extends StatelessWidget {
  const StepThirdPerson({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
